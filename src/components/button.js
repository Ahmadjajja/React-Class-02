function Button(props) {
    // console.log(props.data2);
    return (
      <div>
        <button>{props.data1.title}</button>
        <br/>
        {/* <button>{}</button> */}
      </div>
    );
  }
  
  export default Button;